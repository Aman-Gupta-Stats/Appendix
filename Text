<details>
  <summary>▶ Click here to reveal cell 1</summary>

  ```python
  # This code stays hidden until you click
  import numpy as np
  print("Hidden until clicked!")



<details>
  <summary>▶ First click to show explanation</summary>
  
  Here’s step 1 of the reveal.

  <details>
    <summary>▶ Second click to show code</summary>

    ```python
    # Step 2 hidden until that second click
    print("Hello again!")
    ```
  </details>
</details>



import ipywidgets as widgets
from IPython.display  import display

# create a collapsible region
coll = widgets.Accordion(children=[
    widgets.HTML(
        '<pre style="font-family:monospace">\n'
        'print("This is hidden until you expand!")\n'
        '</pre>'
    )
])
coll.set_title(0, '▶ Click to expand code')
display(coll)
