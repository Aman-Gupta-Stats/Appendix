     # --- concat everything ---
     result = pd.concat(blocks, axis=1)
-    result.index.name = "score_band"
-    return result
+    # 1) move the index into a column
+    result = result.reset_index()  
+
+    # 2) build a new MultiIndex for columns,
+    #    inserting ("", "score_band") as the first tuple
+    #    and then tacking on all the existing (period,metric) pairs
+    existing = list(result.columns[1:])  # these are already tuples
+    new_cols = pd.MultiIndex.from_tuples(
+        [("", result.columns[0])] + existing
+    )
+    result.columns = new_cols
+    return result